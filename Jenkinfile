pipeline{
	agent any
	stages{
		stage("clone"){
			echo "Cloning is Done....."
			}
		}
		stage("build"){
			echo "building package is done"
			}
		}
		stage("test"){
			echo "testing of the package is done........."
			}
		}
		stage("deploy"){
			echo "deployment is done"
			}
		}
	}
}
